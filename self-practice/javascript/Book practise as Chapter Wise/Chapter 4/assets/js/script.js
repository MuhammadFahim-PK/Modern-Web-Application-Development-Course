console.log("Js code Loaded");


//  Variable Names Legal and Illegal


// Illegal variable names (these will cause errors, so keep them as comments)
// var 1name;        // cannot start with number
// var my-name;      // hyphen not allowed
// var var;          // reserved keyword
// var user name;    // space not allowed
// var @price;       // special character not allowed
// var _price;       // underscore allowed


//  A variable name can't contain any spaces.
//  A variable name can contain only letters, numbers, dollar signs, and underscores.
//  Though a variable name can't be any of JavaScript's keywords, it can contain keywords.
//  For example, userAlert and myVar are legal.
//  Capital letters are fine, but be careful.Variable


// 2. Declare 5 legal & 5 illegal variable names

// Legal variable names
var myName;
var _age;
var $price;
var user1;
var first_name;

// Illegal variable names (these will cause errors, so keep them as comments)
// var 1name;        // cannot start with number
// var my-name;      // hyphen not allowed
// var var;          // reserved keyword
// var user name;    // space not allowed
// var @price;       // special character not allowed


// Task 3
document.write("<h3>Rules for naming JS variables</h3>");

document.write("<p>Variable names can only contain alphabets, numbers, $ and _. For example $my_1stVariable</p>");

document.write("<p>Variables must begin with a letter, $ or _. For example $name, _name or name</p>");

document.write("<p>Variable names are case sensitive</p>");

document.write("<p>Variable names should not be JS reserved keywords</p>");